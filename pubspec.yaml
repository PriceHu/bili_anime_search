name: bili_anime_search
description: A simple and basic anime search tool for BiliBili.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.1.210801+14

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  cached_network_image: ^3.0.0
  url_launcher: ^6.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/